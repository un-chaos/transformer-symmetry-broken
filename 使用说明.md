# 使用说明（不用懂代码）

这个项目研究一个问题：**给 Transformer 的 embedding 加一个固定的偏置 `b`，
会打破 attention 原有的「旋转对称性」，这么做训练会变好还是变差？**

你不需要看懂代码，只要会「双击」和「输入数字」就能跑实验。

---

## 一、最快上手（3 步）

### 第 1 步：双击 `run.bat`

就在这个文件夹里，找到 **`run.bat`**，双击它。

> 会弹出一个黑色窗口，里面是中文菜单。
> 如果提示「找不到 python」，说明电脑还没装 Python：去
> <https://www.python.org/downloads/> 下载安装，安装时**一定要勾选
> `Add python.exe to PATH`**，装完再双击 `run.bat`。

> 不喜欢双击也行：在 VS Code 里打开 `run.py`，按右上角的运行按钮；或者在终端里
> 输入 `python run.py`。效果完全一样。

### 第 2 步：输入 `1`，回车

菜单长这样：

```
  1) 跑三种 bias 做对比（b=0 / 高斯 / 常数）   ← 第一次用选这个
  2) 只跑一种 bias
  3) 按 my_config.py 里的设置跑（可自由调参数）
  4) 打开 my_config.py 调参数（自由配置）
  5) 看已有结果（出对比图）
  6) 打开结果文件夹
  7) 查看全部参数 / 说明
  8) 下载大规模语料（FineWeb-Edu 10B，可断点续传）
  0) 退出
```

第一次就输入 **`1`**，然后它会再问你「跑多久」：

```
  1) 玩具任务 —— 不用下载数据，每次约 15 秒
  2) 快速看看 —— 2000 句真实翻译数据，每次约 1 分钟
  3) 认真跑一次 —— 8000 句，每次约 8 分钟
  4) 大规模语料 —— FineWeb-Edu 10B，需要先下载
  （直接按回车 = 2）
```

**第一次建议输入 `1`（玩具任务，15 秒，不用联网）** 先看看流程通不通。
没问题以后再选 `2` 跑真实数据（三种 bias 各跑一遍，加上出图，总共约 4 分钟）。

### 第 3 步：看结果

跑完它会**自动**生成一份对比图和一份中文报告，并把结果文件夹打开给你看：

| 文件 | 是什么 |
|---|---|
| `runs\_compare\compare.png` | **对比图**：三条曲线分别是三种 bias 的验证损失 |
| `runs\_compare\compare.txt` | **中文报告**：一张表格 + 「怎么看」的结论 |
| `runs\_compare\compare.csv` | 同样的表格，可以直接用 Excel 打开 |

**怎么看**：验证损失（val loss）**越低越好**。如果三条曲线几乎重合，说明在这个
规模下三种 bias 差别不大 —— 报告里会直接这么告诉你，并给出下一步建议。

> 小提示：`runs\` 里**已经有一次跑好的对比结果**（就是「快速」模式下这三种 bias）。
> 所以你现在就可以运行 `run.bat` 选菜单 **`5`**，直接看看对比图和报告长什么样，
> 不用先等训练。看完想自己跑，再选 `1` 或 `2`。
>
> 想清空重来：把 `runs\` 文件夹里的子文件夹删掉就行（`_compare` 也可以删）。

---

## 二、每次训练都会保存什么？

这是重点：**训练结果一定会落盘，不会只在屏幕上闪一下就没了。**
每个实验在 `runs\<实验名>\` 里留下这些东西：

| 文件 | 是什么 |
|---|---|
| `training_curve.png` | **损失曲线图**：训练损失 + 验证损失随步数的变化 |
| `training_log.csv` | 完整数据（每一步的 loss、耗时等），Excel 能打开 |
| `model_summary.txt` | **模型说明书**：结构、维度、参数量（下面详细讲） |
| `config.json` | 这次用的模型设置（完全可复现） |
| `bias.json` | 这次 bias 是怎么设的（模式、种子、缩放、落在哪一层） |
| `args.json` | 这次用的全部命令行参数 |
| `model_final.pt` / `model_best.pt` | 模型权重（占地方最大，删了不影响看曲线） |

训练时屏幕上也会先打印一遍「模型说明书」，然后每 `log_every` 步打印一行进度。

### 模型说明书长什么样？

每次训练一开始，程序会打印它，并同时写进 `model_summary.txt`。以 `tiny` 模型为例
（真实输出，节选）：

```
Architecture
  n_embd (d_model)     : 128
  n_head               : 4   (head_dim 32)
  layers               : encoder 2, decoder 2
  d_ff (feed-forward)  : 256
  context_length       : 32
  vocab                : source 400, target 400
  tie_output_embedding : True
  symmetry-breaking    : embedding b: mode=gaussian std=0.02 ...

Components (learned parameters)
  source embedding
      params        55,296  ( 7.2%)   dims   embed 400x128, pos 32x128
  encoder layer 0-1 (x2)
      params       264,960  (34.3%)   dims   each: q/k/v/o 128x128 (+4 bias), ff 128->256, ln 128 x2
  decoder layer 0-1 (x2)
      params       397,568  (51.4%)   dims   each: q/k/v/o 128x128 x2 (+8 bias), ff 128->256, ln 128 x3
  output projection
      params             0  ( 0.0%)   dims   (shared / tied)
  TOTAL              773,120  (100.0%)

Where the parameters live
  embeddings                  110,592    14.3%
  encoder                     264,960    34.3%
  decoder                     397,568    51.4%

Non-learned buffers: 128 values in 1 tensor(s) (these are NOT trained)
  src_embed.bias.b                128
```

它一次告诉你四件事：**结构**（几层、多宽、几个头）、**每部分多少参数**、
**参数花在哪**（encoder 还是 decoder 还是 embedding）、以及**哪些是「不训练的」
缓冲区**（本项目的偏置 `b` 就是缓冲区 —— 它是固定的研究对象，不参与训练）。

> 上面 `output projection` 那行是 0：因为 `tie_output_embedding=True`（默认），
> 输出层和词嵌入共用一份权重。想要独立的输出层就在 `my_config.py` 里写
> `"--no_tie_output_embedding": True`，那时这一行会显示它自己的参数。

---

## 三、想换一个设置再跑一次

有四个层次，**从简单到复杂**，按需选。

### 层次 1：什么都不用改（推荐）

再运行一次 `run.bat`，选菜单 **`2`**，然后用数字选：
- 「跑多久」（1/2/3/4）
- 「用哪种 bias」：

| 编号 | 名称 | 含义 |
|---|---|---|
| 1 | `symmetric` | **对照组**：`b = 0`，完全不破缺对称性 |
| 2 | `b-gaussian` | **高斯随机 b**：给 embedding 加一个随机的固定偏置（推荐先看这个） |
| 3 | `b-const` | **常数 b**：每个维度都加同一个常数 |
| 4 | `attn-bQbV` | attention 内部的 bQ+bV（参考项目那种做法） |
| 5 | `attn-full` | attention 内部的 bQ+bK+bV（三个方向全加） |
| 6 | `b-learnable` | 可学习的 b：b 不固定，交给优化器去学 |

跑完照样自动出曲线图 —— **这就是「换个 config 再跑一下看看效果」的最快操作**。

### 层次 2：自由调参数（不写死任何东西）

想改初始参数（模型多大、学习率、batch size、种子、数据集…）就走这里：

1. 运行 `run.bat`，选菜单 **`4`** —— 它会打开 **`my_config.py`**；
2. 在里面的 `SETTINGS` 里改数值，保存；
3. 回菜单选 **`3`**，它就按你的设置跑。

`my_config.py` 长这样（每一项都有中文注释）：

```python
FORMAT = 2

SETTINGS = {
    "--dataset_preset": 'multi30k-quick',   # 数据集
    "--objective": None,                    # translation / denoising
    "--model": 'tiny',                      # smoke / tiny / small / base / large
    "--bias_preset": 'b-gaussian',          # 对称性破缺设置
    "--optimizer": 'egd',                   # egd / adamw / sgdm
    "--egd_lr": None,                       # EGD 学习率
    "--egd_F0": None,                       # EGD 的 loss 偏移
    "--batch_size": None,                   # 每批多少条
    "--epochs": 3,                          # 训练几轮
    "--max_steps": None,                    # 最多多少步（0 = 由 epochs 决定）
    "--ctx": None,                          # 上下文长度
    "--n_embd": None,                       # 隐藏维度
    "--n_head": None,                       # 注意力头数
    "--log_dir": 'runs',                    # 结果放哪
    "--name": 'quick-bgaussian',            # 实验名字
}

EXTRA_ARGS = []
```

规则很简单：

- `SETTINGS` 里每一项都是**真实的命令行参数**（`--xxx`）。`train.py` 支持多少参数，
  这里就能写多少 —— **没有任何参数是写死的**。
- 值就是 Python 的值：数字写 `0.1`，字符串写 `"abc"`，开关写 `True` / `False`。
- 写 `None` 表示「这个参数我不指定，用默认值」。
- 想用的参数不在 `SETTINGS` 里？文件最下面有一份**自动生成的完整参数清单**
  （从 `train.py` 真实读取，不会过期），把那一行搬上来就行。
- 实在不会写：`EXTRA_ARGS` 是万能兜底，直接写一串命令行参数，原样追加到最后，
  例如 `EXTRA_ARGS = ["--seed", "123", "--grad_clip", "1.0"]`。

> 改坏了也没关系（比如少了个引号）：程序会提示「`my_config.py` 读不了」，
> 然后照常用菜单选，不会崩。菜单 **`4`** 随时能重新生成一份新的。
>
> 菜单 **`7`** 里能直接查看**全部参数和一句话说明**（中文），不用去翻代码。

### 层次 3：命令行（可选）

不喜欢菜单就直接用命令行，等价于菜单选的东西：

```
python main.py train --help                     全部参数
python main.py train --model tiny --dataset_preset multi30k-quick ^
    --bias_preset b-gaussian --name quick-bgaussian
```

`run.py` 也支持跳过菜单直接跑：

```
python run.py --speed quick --bias b-gaussian     # 等价于菜单 2)
python run.py --from-config                       # 等价于菜单 3)
python run.py --dry-run                           # 只打印命令，不真跑
```

### 层次 4：跑大规模语料（FineWeb-Edu 10B）

想用真正大的数据（**约 10B 词、14 个分片、约 28.5 GB**）训练：

1. 运行 `run.bat`，选菜单 **`8`**。
2. 它会先显示「已经下了多少、还差多少」，然后问你要不要开始。
3. 回答「是」之后，它**会另开一个窗口**开始下载 —— 你可以盯着那个窗口看进度，
   卡住了还是在下都看得见。下载是**断点续传**的：中途关了、断网了，
   再选一次 `8` 就接着下，不会从头再来。
4. 下完（或下了一部分）后，在菜单 `1`/`2` 里把「跑多久」选成 **`4`（大规模语料）**，
   或者用 `my_config.py` 指定：

```python
SETTINGS = {
    "--dataset_preset": 'fineweb-10b',      # 或 fineweb-quick（先小试）
    "--objective": 'denoising',             # 原始文本用 denoising（T5 式去噪）
    "--fineweb_dir": 'data/fineweb-edu/sample-10BT',
    "--bpe_vocab_size": 32000,              # BPE 词表大小
    "--tokenizer_train_documents": 20000,   # 用多少篇文档训分词器
    "--max_documents": 0,                   # 用多少篇文档（0 = 全部）
    "--max_steps": 2000,                    # CPU 上建议先限制步数
}
```

命令行等价写法：

```
python main.py download-data                # 下载（断点续传）
python main.py download-data --status       # 只看下到哪了
```

> **CPU 上别硬跑 10B。** 训练是在 CPU 上做的，跑全量 10B 会非常慢。
> 建议先用 `--max_steps 200` 试通流程，再决定要不要加大。
> 数据集太大时先用 `--max_documents` 限制文档数。

**translation 和 denoising 有什么区别？**

| | 数据要求 | 说明 |
|---|---|---|
| `translation` | 平行语料（Multi30k：英文 + 德文） | 传统翻译任务，有 BLEU 分数 |
| `denoising` | 随便什么原始文本（FineWeb-Edu） | T5 式「挖空再补」，**不需要翻译对照**，所以能用超大单语语料 |

---

## 四、常见问题

**Q：没网能跑吗？**
能。选「玩具任务」（`synthetic-*`），它是程序自己生成的假数据，不下载任何东西。
真实数据第一次需要联网（Multi30k 从 HuggingFace 镜像下载，FineWeb-Edu 从
HuggingFace 下载），之后会缓存在 `data\` 文件夹里。
（如果下载失败，程序会打印一行警告并自动改用合成数据，不会直接崩。）

**Q：太慢了怎么办？**
- 「跑多久」换小一号（4 → 3 → 2 → 1）；
- 或者编辑 `my_config.py`：把 `--max_steps` 写个小数字（比如 `200`），
  或者把 `--epochs` 改小、`--model` 换成 `smoke`/`tiny`。

**Q：跑完结果在哪？**
全在 `runs\` 文件夹里。每个实验一个子文件夹（比如 `runs\quick-bgaussian\`），
里面是它的**损失曲线图**、日志、模型说明书、配置和权重；汇总的对比图和报告在
`runs\_compare\`。菜单 **`6`** 可以直接把文件夹打开。

**Q：为什么看不到曲线图？**
只要是用 `run.bat` 跑的，`runs\<名字>\training_curve.png` **一定**会生成
（画图是硬性步骤，没有「跳过画图」的开关）。如果你是用命令行自己跑的，
别加 `--no_plot` 就能生成。

**Q：模型到底多大？参数都花在哪了？**
看 `runs\<名字>\model_summary.txt`，或者训练开头的屏幕输出。见第二节。

**Q：不想训练，只想重新看对比图？**
运行 `run.bat`，选菜单 **`5`**。它会扫描 `runs\` 里所有跑完的实验，
重新生成对比图和报告。

**Q：想删掉旧结果？**
直接删 `runs\` 里对应的子文件夹就行。删了就不会出现在对比图里了。
`runs\<名字>\model_*.pt` 是模型权重（占地方最大的），删了不影响看曲线。

**Q：`--max_seconds`、`--seed` 这些是什么？**
进阶选项，菜单里用不到，但**都可以写在 `my_config.py` 里**。
想看全部：菜单 `7`，或 `python main.py train --help`。

**Q：跑一半中断了，能接着跑吗？**
能，用命令行（菜单里暂时没做）：

```
python main.py train --model tiny --dataset_preset multi30k-quick ^
    --bias_preset b-gaussian --name quick-bgaussian --log_dir runs ^
    --resume runs\quick-bgaussian\model_final.pt
```

（`^` 是 Windows 命令行的换行符，也可以写成一行。）

---

## 五、名词速查

| 词 | 意思 |
|---|---|
| **bias / b** | 加在 embedding（或 attention）上的固定偏置。本项目的核心研究对象 |
| **对称性破缺** | `b = 0` 时，把 embedding 整体旋转一下模型行为不变（旋转对称性）。`b ≠ 0` 就把这个对称性打破了 |
| **val loss（验证损失）** | 在没训练过的数据上的预测误差。**越低越好** |
| **train loss（训练损失）** | 训练数据上的误差，参考用，不能和验证损失直接比大小 |
| **BLEU** | 机器翻译质量打分，**越高越好**（0~100）。短训练里通常是 0，不用慌 |
| **epoch（轮）** | 把训练数据完整过一遍叫一轮 |
| **参数量** | 模型大小。越大通常越好，但越慢 |
| **EGD** | 本项目的优化器：能量守恒下降法。对照的是常规的 AdamW |
| **attention** | Transformer 内部的「注意力」机制，bias 就是加在它的 q/k/v 上的 |
| **denoising（去噪）** | 把句子里几段挖掉换成占位符，让模型补回来。这样单语文本也能用来训练 |
| **BPE** | 一种把文本切成子词的方法，用来处理超大语料 |

---

## 六、想更深入了解

- **项目背景、原理、完整参数表**：看 `README.md`（英文）。
- **所有命令行选项**：菜单 `7`，或 `python main.py train --help`
- **代码结构**：`symbreak_transformer/`（每个文件一个模块，encoder 和 decoder 是分开的）
- **测试**：`python -m pytest tests -q`

> 内部实现可以很复杂（模型、优化器、数据管线、对称性度量都拆成了独立文件），
> 但**你日常只需要 `run.bat` + 数字**。
> 想改初始参数就编辑 `my_config.py`（菜单 `4` 打开），想用大数据就选菜单 `8`。
