# 使用说明（不用懂代码）

这个项目研究一个问题：**给 Transformer 的 embedding 加一个固定的偏置 `b`，
会打破 attention 原有的「旋转对称性」，这么做训练会变好还是变差？**

**你只需要会「双击」和「输入数字」。** 这个项目只有一种用法：

```
双击 run.bat  →  输入数字  →  回车
```

没有命令行、没有配置文件、不需要看懂任何代码。

---

## 一、最快上手（3 步）

### 第 1 步：双击 `run.bat`

就在这个文件夹里，找到 **`run.bat`**，双击它。

> 会弹出一个黑色窗口，里面是中文菜单。
> 如果提示「找不到 python」，说明电脑还没装 Python：去
> <https://www.python.org/downloads/> 下载安装，安装时**一定要勾选
> `Add python.exe to PATH`**，装完再双击 `run.bat`。

> 不喜欢双击也行：在 VS Code 里打开 `run.py`，按右上角的运行按钮；或者在终端里
> 输入 `python run.py`。**效果完全一样**，这就是同一个用法。

### 第 2 步：输入 `1`，回车

菜单长这样：

```
  1) 开始训练：三种 bias 做对比（b=0 / 高斯 / 常数）   ← 第一次用选这个
  2) 开始训练：只跑一种 bias
  3) 修改训练设置（模型档位 / 结构 / bias / 全部参数）
  4) 查看当前设置
  5) 看已有结果（重新出对比图和报告）
  6) 分析 bias 到底改变了什么（挑一个训练好的模型）
  7) 打开结果文件夹
  8) 大规模语料：已就绪（14 个分片 / 28.5 GB），训练直接使用
  9) 帮助：每个选项是什么意思
  0) 退出
```

> 第 8 项会**根据你本地的实际情况**自动改写：已经下好了就直接写「已就绪，
> 训练直接使用」；不完整时才会提示可以续传补齐；完全没有时才提示下载。

第一次就输入 **`1`**，然后它会问「跑多久」：

```
  1) 玩具任务 —— 不用下载数据、不用联网，每次约 15 秒  ← 第一次用选这个
  2) 快速看看 —— 2000 句真实翻译数据，每次约 1 分钟（要联网）
  3) 认真跑一次 —— 8000 句，每次约 8 分钟
  4) 大规模语料 —— FineWeb-Edu 10B（菜单 8 先下载；CPU 上很慢）
```

**第一次建议直接回车**（= 玩具任务，15 秒，不用联网），先看看流程通不通。
没问题以后再选 `2` 跑真实数据（三种 bias 各跑一遍，加上出图，总共约 4 分钟）。

### 第 3 步：看结果

跑完它会**自动**生成一份对比图和一份中文报告，并问你要不要打开结果文件夹：

| 文件 | 是什么 |
|---|---|
| `runs\_compare\compare.png` | **对比图**：三条曲线分别是三种 bias 的验证损失 |
| `runs\_compare\compare.txt` | **中文报告**：一张表格 + 「怎么看」的结论 |
| `runs\_compare\compare.csv` | 同样的表格，可以直接用 Excel 打开 |

**怎么看**：验证损失（val loss）**越低越好**。如果三条曲线几乎重合，说明在这个
规模下三种 bias 差别不大 —— 报告里会直接这么告诉你，并给出下一步建议。

> 小提示：`runs\` 里**已经有一次跑好的对比结果**。所以你现在就可以双击
> `run.bat` 选菜单 **`5`**，直接看看对比图和报告长什么样，不用先等训练。
> 看完想自己跑，再选 `1` 或 `2`。
>
> 想清空重来：把 `runs\` 文件夹里的子文件夹删掉就行（`_compare` 也可以删）。

---

## 二、每次训练都会保存什么？

**训练结果一定会落盘，不会只在屏幕上闪一下就没了。**
每个实验在 `runs\<实验名>\` 里留下这些东西：

| 文件 | 是什么 |
|---|---|
| `training_curve.png` | **损失曲线图**：训练损失 + 验证损失随步数的变化 |
| `model_summary.txt` | **模型说明书**：结构、维度、参数量（下面详细讲） |
| `training_log.csv` | 完整数据（每一步的 loss、耗时等），Excel 能打开 |
| `config.json` | 这次用的模型设置（完全可复现） |
| `bias.json` | 这次 bias 是怎么设的（模式、种子、缩放、落在哪一层） |
| `args.json` | 这次用的全部参数 |
| `model_final.pt` / `model_best.pt` | 模型权重（占地方最大，删了不影响看曲线） |

训练时屏幕上也会先打印一遍「模型说明书」，然后每隔几步打印一行进度。

### 模型说明书长什么样？

每次训练一开始，程序会打印它，并同时写进 `model_summary.txt`。以 `tiny` 模型为例
（真实输出，节选）：

```
Architecture
  n_embd (d_model)     : 128
  n_head               : 4   (head_dim 32)
  layers               : encoder 2, decoder 2
  d_ff (feed-forward)  : 256
  context_length       : 32
  vocab                : source 400, target 400
  tie_output_embedding : True
  symmetry-breaking    : embedding b: mode=gaussian std=0.02 ...

Components (learned parameters)
  source embedding
      params        55,296  ( 7.2%)   dims   embed 400x128, pos 32x128
  encoder layer 0-1 (x2)
      params       264,960  (34.3%)   dims   each: q/k/v/o 128x128 (+4 bias), ff 128->256, ln 128 x2
  decoder layer 0-1 (x2)
      params       397,568  (51.4%)   dims   each: q/k/v/o 128x128 x2 (+8 bias), ff 128->256, ln 128 x3
  output projection
      params             0  ( 0.0%)   dims   (shared / tied)
  TOTAL              773,120  (100.0%)

Where the parameters live
  embeddings                  110,592    14.3%
  encoder                     264,960    34.3%
  decoder                     397,568    51.4%

Non-learned buffers: 128 values in 1 tensor(s) (these are NOT trained)
  src_embed.bias.b                128
```

它一次告诉你四件事：**结构**（几层、多宽、几个头）、**每部分多少参数**、
**参数花在哪**（encoder 还是 decoder 还是 embedding）、以及**哪些是「不训练的」
缓冲区**（本项目的偏置 `b` 就是缓冲区 —— 它是固定的研究对象，不参与训练）。

「参数量」就是模型大小 —— 越大通常越强，但越慢。

---

## 三、想换设置再跑：菜单 3)

**不需要改任何文件。** 菜单选 **`3`**，它先告诉你**当前模型有多大**，然后列出常用设置：

```
  当前模型规模：≈ 1.2M 参数（按 8000 词表估算；真实值训练一开始会打印）
   1) --dataset_preset         = fineweb-quick
   2) --objective              = （默认）
   3) --model                  = 4m
   4) --n_embd                 = （默认）
   ...
  11) --bias_const             = （默认）
   ...
   m) 模型结构（档位 / 宽度 / 层数 / 头数 / 上下文长度）
   b) 对称性破缺（bias 的详细设置，含「常数 b 的值」）
   a) 全部参数（train.py 支持的所有设置）
   d) 全部恢复默认
```

输入编号就能改：

- **有固定选项的**（数据集、模型档位、优化器……）它会把选项列出来，你输入编号；
- **要填数字的**（学习率、batch size、b 的值、维度……）你直接输入数值；
- **开关类的**它问 y/n；
- **输入空、直接回车 = 恢复默认**（也就是"这项我不改"）。

改过的项前面会有 **★**，而且**程序会自动记住**，下次打开 `run.bat` 还在。

### 3.1 模型结构：`m)`

选 **`m`** 进「模型结构」界面，每一项都带一句中文说明：

```
  当前模型规模：≈ 1.2M 参数（按 8000 词表估算）
   1) --model                  = 4m
      档位：直接按参数量选（4m/10m/25m/50m/124m），或按预设选（smoke…large）
   2) --n_embd                 = （默认）
      隐藏维度：模型有多宽。调大 → 参数变多、变慢、一般更强
   3) --n_head                 = （默认）
      注意力头数：必须能整除隐藏维度
   4) --n_encoder_layer        = （默认）
      编码器层数（读输入的那半边）
   ...（还有解码器层数、前馈宽度、上下文长度、dropout、激活函数等）
```

**按参数量选档位**（选第 1 项 `--model`）：

```
   1) 4m      ≈   1.2M 参数   （参数量档位）
   2) 10m     ≈   4.0M 参数   （参数量档位）
   3) 25m     ≈  13.4M 参数   （参数量档位）
   4) 50m     ≈  31.4M 参数   （参数量档位）
   5) 124m    ≈  97.0M 参数   （参数量档位）
   6) smoke   ≈   0.4M 参数   （预设）
   7) tiny    ≈   0.7M 参数   （预设）
   ...
```

> **为什么数字和档位名字不完全一样？** 档位名字（4m / 124m）是按 **32000 的
> BPE 词表**命名的一个标准参照；而**实际参数量取决于你的数据会建出多大的词表**
> —— 词嵌入通常占大头。上面的数字是按你当前数据估算出来的**真实规模**，
> 所以换数据集时它会跟着变（比如 `fineweb-quick` 用 8000 词表，同样的结构就小
> 很多）。训练一开始打印的 `model_summary.txt` 才是最准的数字。
>
> 你也可以完全不选档位，直接改宽度/层数/头数——界面顶部的估算会**立刻更新**，
> 你能马上看到「这么改会变成多大」。

### 3.2 常数 b：`b)`

选 **`b`** 进「对称性破缺（bias）」界面。想让 b 是一个**你自己指定的常数**，
两步：

1. 把 `--bias_mode` 设成 `const`（输入编号选）；
2. 改 `--bias_const`，**直接输入你要的数**（例如 `2.5`；默认是 `1.0`）。

```
   1) --bias_preset            = b-const
      一键方案：symmetric / b-gaussian / b-const / attn-bQbV / ...
   2) --bias_mode              = const
      embedding 上 b 的生成方式：zero / gaussian / const
   3) --bias_const             = 2.5
      **常数 b 的值**：mode=const 时每个维度都加这个数
   4) --bias_std               = （默认）
      高斯 b 的标准差（默认 0.02，越大破缺越强）
   ...（还有均值、随机种子、是否可学习、Q/K/V 上是否加偏置、注意力常数等）
```

> 常数 b 的效果可以核对：`runs\<名字>\training_log.csv` 里的 `embed_b_norm`
> 就等于 `b的值 × √隐藏维度`（例如 b=2.5、隐藏维度 64 → 20.0），
> `bias.json` 里也会记下你填的数。

> **没有任何参数是写死的。** `train.py` 支持多少参数，这里就能改多少 —— 选 **`a`**
> 会按分组列出**全部**参数（100 多项），同样输入编号就能改。
> 唯一的例外是「不生成损失曲线」那个开关：曲线是本项目的硬性要求，所以没放进菜单。

想确认现在到底会用哪些参数，回菜单选 **`4`**，它会把这次真正传给训练的每一个
参数原样列出来。

改完设置，回菜单选 `1`（三种对比）或 `2`（只跑一种）就开始训练。

---

## 四、分析：这个 bias 到底改变了什么？（菜单 6）

菜单 **`6`** 会拿一个**训练好的模型**，把里面的 bias 去掉，比较去掉前后的输出：

```
  分析哪一个？
  1) toy-bgaussian
  2) toy-symmetric
```

跑完会打印一行结果，并存成 `runs\<名字>\bias_analysis.json`：

```
effect : logit_rel_change=4.2227e-02  loss_with=2.7114  loss_without=2.7125
```

**怎么读**：`logit_rel_change` 是「把 bias 去掉以后，模型的输出改变了多少」。
**这个数字越大，说明这个 bias 对模型的行为越重要**（也就越说明对称性真的被破坏了）。
如果它接近 0，说明这个 bias 装上去跟没装一样。

> 这是「对称性破缺到底有没有用」的量化答案 —— 和菜单 1) 的损失对比图配合着看：
> 一个说「效果好不好」，一个说「模型到底有没有用上这个 bias」。

---

## 五、大规模语料（FineWeb-Edu 10B）

**约 10B 词、14 个分片、约 28.5 GB。本地已经有了，训练直接用，不用再下。**

1. 菜单里第 8 项就已经写着状态：
   `大规模语料：已就绪（14 个分片 / 28.5 GB），训练直接使用`
2. 想用这份语料训练：菜单 `3` 把 `--dataset_preset` 改成
   `fineweb-quick`（8000 词表的小试档）或 `fineweb-10b`（全量），
   再选 `1` 或 `2` 开始训练。训练会**直接读** `data\fineweb-edu\sample-10BT\`。
3. 如果哪天第 8 项显示「不完整」，选它就会**续传补齐**（会另开一个窗口显示进度，
   你能看见是卡住了还是在下）；补几次都行，已经下好的分片不会重下。
4. 只有本地一片都没有时才需要真正下载。

> 选第 8 项时，如果语料已经完整，它只会打印一行「已就绪」然后返回 ——
> **不会联网、不会启动下载**（除非你回答「要连网重新核对」）。

> **CPU 上别硬跑 10B。** 训练是在 CPU 上做的，跑全量 10B 会非常慢。
> 建议在菜单 3 里把 `--max_steps` 写个小数字（比如 `200`）先试通流程，
> 或者用 `m` 把档位换成 `4m` 之类的小模型。
>
> 这个语料是**纯英文**的，没有翻译对照，所以它自动使用 denoising（去噪）任务：
> 把句子里几段挖掉换成占位符，让模型补回来。翻译任务（multi30k）才需要平行语料。

---

## 六、常见问题

**Q：有没有别的用法？比如命令行？**
没有。这个项目**只有**一种用法：双击 `run.bat`，输入数字。
所有功能都在菜单里（改参数在菜单 3、看结果在菜单 5、分析 bias 在菜单 6、语料在菜单 8）。
这样你不用记任何命令，也不会因为用错方式而出错。

**Q：我的设置存在哪个文件里？要自己维护吗？**
不用。程序把「跑多久」「哪种 bias」和你手改过的参数自动记在一个内部文件里，
下次打开自动恢复。**你不需要打开、也不需要编辑它** —— 想改就在菜单 3 里改，
想全部还原就选菜单 3 的 `d`。

**Q：语料已经下载好了，为什么还要我再下一次？**
不用再下了 —— 现在菜单会**先看本地**：已经下好就直接写「已就绪，训练直接使用」，
选第 8 项也只会报告一下，不会启动下载（想强制联网核对，它才会去）。
训练本身是直接读本地目录的，从来不会自己触发下载。
（`data\fineweb-edu\sample-10BT\` 里那 14 个分片就是它在用的东西。）

**Q：没网能跑吗？**
能。菜单 1 里的「玩具任务」是程序自己生成的假数据，不下载任何东西。
`data\` 里已经缓存好的数据（Multi30k、FineWeb-Edu）也不需要联网 ——
只有当你确实缺数据、并且主动去下载时才会用到网络。
（如果下载失败，程序会打印一行警告并自动改用合成数据，不会直接崩。）

**Q：太慢了怎么办？**
- 「跑多久」换小一号（4 → 3 → 2 → 1）；
- 或者菜单 3 里把 `--max_steps` 写个小数字（比如 `200`）、
  把 `--epochs` 改小；
- 或者菜单 3 → `m` 里把档位换小（`124m` → `25m` → `4m`）。
  界面顶部会显示换完之后大约多少参数。

**Q：怎么把模型改大 / 改小？**
菜单 3 → `m`（模型结构）：可以直接按参数量选档位（4m / 10m / 25m / 50m / 124m），
也可以自己填宽度、层数、头数、前馈宽度。顶部一直显示「当前 ≈ 多少参数」。

**Q：怎么让 b 是一个我自己定的常数？**
菜单 3 → `b`：先把 `--bias_mode` 设成 `const`，再改 `--bias_const`
（直接输入你要的数，比如 `2.5`）。见第 3.2 节。

**Q：跑完结果在哪？**
全在 `runs\` 文件夹里。每个实验一个子文件夹（比如 `runs\quick-bgaussian\`），
里面是它的**损失曲线图**、日志、模型说明书、配置和权重；汇总的对比图和报告在
`runs\_compare\`。菜单 **`7`** 可以直接把文件夹打开。

**Q：为什么看不到曲线图？**
只要是按菜单跑的，`runs\<名字>\training_curve.png` **一定**会生成 ——
画图是硬性步骤，菜单里没有关掉它的选项。

**Q：模型到底多大？参数都花在哪了？**
菜单 3 和 4 会显示估算值；训练开头会打印**真实**的
`runs\<名字>\model_summary.txt`，里面写明每部分多少参数。见第二节。

**Q：不想训练，只想重新看对比图？**
菜单 **`5`**。它会扫描 `runs\` 里所有跑完的实验，重新生成对比图和报告。

**Q：想删掉旧结果？**
直接删 `runs\` 里对应的子文件夹就行。删了就不会出现在对比图里了。
`runs\<名字>\model_*.pt` 是模型权重（占地方最大的），删了不影响看曲线。

**Q：程序突然出错了怎么办？**
它不会整个崩掉：会打印出错原因，然后回到菜单，你可以接着选（比如换成
「玩具任务」先跑通）。真想知道细节，看它打印的报错信息。

---

## 七、名词速查

| 词 | 意思 |
|---|---|
| **bias / b** | 加在 embedding（或 attention）上的固定偏置。本项目的核心研究对象 |
| **对称性破缺** | `b = 0` 时，把 embedding 整体旋转一下模型行为不变（旋转对称性）。`b ≠ 0` 就把这个对称性打破了 |
| **val loss（验证损失）** | 在没训练过的数据上的预测误差。**越低越好** |
| **train loss（训练损失）** | 训练数据上的误差，参考用，不能和验证损失直接比大小 |
| **BLEU** | 机器翻译质量打分，**越高越好**（0~100）。短训练里通常是 0，不用慌 |
| **epoch（轮）** | 把训练数据完整过一遍叫一轮 |
| **step（步）** | 训练一次（处理一批数据）叫一步 |
| **参数量** | 模型大小。越大通常越好，但越慢 |
| **EGD** | 本项目的优化器：能量守恒下降法。对照的是常规的 AdamW |
| **attention** | Transformer 内部的「注意力」机制，bias 就是加在它的 q/k/v 上的 |
| **denoising（去噪）** | 把句子里几段挖掉换成占位符，让模型补回来。这样单语文本也能用来训练 |
| **BPE** | 一种把文本切成子词的方法，用来处理超大语料 |

---

## 八、给想看代码的人

- **项目背景、原理、完整参数表**：看 `README.md`（英文）。
- **代码结构**：`symbreak_transformer/`（每个文件一个模块，encoder 和 decoder 是分开的）
- **跑测试**：`python -m pytest tests -q`

> 内部实现可以很复杂（模型、优化器、数据管线、对称性度量都拆成了独立文件），
> 但对使用者来说**只有菜单一个入口** —— 这是刻意的设计。
