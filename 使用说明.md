# 使用说明（不用懂代码）

这个项目是用来研究一个问题：**给 Transformer 的 embedding 加一个固定的偏置 `b`，
会打破 attention 原有的「旋转对称性」，这么做训练会变好还是变差？**

你不需要看懂代码，只要会「双击」和「输入数字」就能跑实验。

---

## 一、最快上手（3 步）

### 第 1 步：双击 `run.bat`

就在这个文件夹里，找到 **`run.bat`**，双击它。

> 会弹出一个黑色窗口，里面是中文菜单。
> 如果提示「找不到 python」，说明电脑还没装 Python：去
> <https://www.python.org/downloads/> 下载安装，安装时**一定要勾选
> `Add python.exe to PATH`**，装完再双击 `run.bat`。

> 不喜欢双击也行：在 VS Code 里打开 `run.py`，按右上角的运行按钮；或者在终端里
> 输入 `python run.py`。效果完全一样。

### 第 2 步：输入 `1`，回车

菜单长这样：

```
  1) 跑三种 bias 做对比（b=0 / 高斯 / 常数）   ← 第一次用选这个
  2) 只跑一种 bias
  3) 按 my_config.py 里的设置跑
  4) 看已有结果（出对比图，不重新训练）
  5) 打开结果文件夹
  6) 我该改什么？（说明）
  0) 退出
```

第一次就输入 **`1`**，然后它会再问你「跑多久」：

```
  1) 玩具任务 —— 不用下载数据，每次约 15 秒
  2) 快速看看 —— 2000 句真实翻译数据，每次约 1 分钟
  3) 认真跑一次 —— 8000 句，每次约 8 分钟
  （直接按回车 = 2）
```

**第一次建议输入 `1`（玩具任务，15 秒，不用联网）** 先看看流程通不通。
没问题以后再选 `2` 跑真实数据（三种 bias 各跑一遍，加上出图，总共约 4 分钟）。

### 第 3 步：看结果

跑完它会**自动**生成一份对比图和一份中文报告，并把结果文件夹打开给你看：

| 文件 | 是什么 |
|---|---|
| `runs\_compare\compare.png` | **对比图**：三条曲线分别是三种 bias 的验证损失 |
| `runs\_compare\compare.txt` | **中文报告**：一张表格 + 「怎么看」的结论 |
| `runs\_compare\compare.csv` | 同样的表格，可以直接用 Excel 打开 |

**怎么看**：验证损失（val loss）**越低越好**。如果三条曲线几乎重合，说明在这个
规模下三种 bias 差别不大 —— 报告里会直接这么告诉你，并给出下一步建议。

> 小提示：`runs\` 里**已经有一次跑好的对比结果**（就是「快速」模式下这三种 bias，
> 花了约 2 分钟）。所以你现在就可以运行 `run.bat` 选菜单 **`4`**，直接看看对比图和
> 报告长什么样，不用先等训练。看完想自己跑，再选 `1` 或 `2`。
>
> 想清空重来：把 `runs\` 文件夹里的子文件夹删掉就行（`_compare` 也可以删）。

---

## 二、想换一个设置再跑一次

有三个层次，**从简单到复杂**，按需选：

### 层次 1：什么都不用改（推荐）

再运行一次 `run.bat`，选菜单 **`2`**，然后用数字选：
- 「跑多久」（1/2/3）
- 「用哪种 bias」：

| 编号 | 名称 | 含义 |
|---|---|---|
| 1 | `symmetric` | **对照组**：`b = 0`，完全不破缺对称性 |
| 2 | `b-gaussian` | **高斯随机 b**：给 embedding 加一个随机的固定偏置（推荐先看这个） |
| 3 | `b-const` | **常数 b**：每个维度都加同一个常数 |
| 4 | `attn-bQbV` | attention 内部的 bQ+bV（参考项目那种做法） |
| 5 | `attn-full` | attention 内部的 bQ+bK+bV（三个方向全加） |
| 6 | `b-learnable` | 可学习的 b：b 不固定，交给优化器去学 |

跑完照样自动出对比图 —— **这就是「换个 config 再跑一下看看效果」的全部操作**。

### 层次 2：固定一套设置，以后一键重跑

每次跑完，程序都会把这次的选择写进 **`my_config.py`**。你可以直接编辑它
（用 VS Code 或记事本打开），里面每一项都有中文注释，比如：

```python
SPEED = "quick"          # toy / quick / long
BIAS = "b-gaussian"      # symmetric / b-gaussian / b-const / attn-bQbV / ...
EPOCHS = 2               # 训练轮数，越大越久
OPTIMIZER = "egd"        # egd（本项目的能量守恒下降法）/ adamw（常规对照）
LOG_DIR = "runs"         # 结果放哪
AUTO_REPORT = True       # 跑完自动出对比图
```

只改引号里的词就行。改完保存，然后运行 `run.bat` 并选菜单 **`3`**。

> 改坏了也没关系（比如少了个引号）：程序会提示「my_config.py 读不了」，
> 然后照常用菜单选，不会崩。

### 层次 3：改更细的东西

模型多大、多少数据、学习率、batch size……这些在
`symbreak_transformer/config.py` 里，或者用命令行看全部选项：

```
python main.py train --help
```

菜单 **`6`** 里也有一份中文说明。

---

## 三、常见问题

**Q：没网能跑吗？**
能。选「玩具任务」（`synthetic`），它是程序自己生成的假数据，不下载任何东西。
真实数据（Multi30k）是从 HuggingFace 镜像下载的，第一次需要联网，之后会缓存在
`data\` 文件夹里。
（如果下载失败，程序会打印一行警告并自动改用合成数据，不会直接崩。）

**Q：太慢了怎么办？**
- 「跑多久」换小一号（3 → 2 → 1）；
- 或者编辑 `my_config.py`，把 `EPOCHS` 改小（比如 2 改成 1）。

**Q：跑完结果在哪？**
全在 `runs\` 文件夹里。每个实验一个子文件夹（比如 `runs\quick-bgaussian\`），
里面是它的日志、配置和模型权重；汇总的图和报告在 `runs\_compare\`。

**Q：不想训练，只想重新看对比图？**
运行 `run.bat`，选菜单 **`4`**。它会扫描 `runs\` 里所有跑完的实验，
重新生成对比图和报告。

**Q：想删掉旧结果？**
直接删 `runs\` 里对应的子文件夹就行。删了就不会出现在对比图里了。
`runs\<名字>\model_*.pt` 是模型权重（占地方最大的），删了不影响看曲线。

**Q：怎么看某个实验的细节？**
菜单 **`4`** 出对比图之后，报告末尾会列出每个实验的目录路径；
`runs\<名字>\training_log.csv` 是完整数据，可以用 Excel 打开。

**Q：`--max_seconds`、`--seed` 这些是什么？**
那是给命令行用的进阶选项，菜单里用不到。想看全部：
`python main.py train --help`。

**Q：跑一半中断了，能接着跑吗？**
能，但要用命令行（菜单里暂时没做）：

```
python main.py train --model tiny --dataset_preset multi30k-quick ^
    --bias_preset b-gaussian --name quick-bgaussian --log_dir runs ^
    --resume runs\quick-bgaussian\model_final.pt
```

（`^` 是 Windows 命令行的换行符，也可以写成一行。）

---

## 四、名词速查

| 词 | 意思 |
|---|---|
| **bias / b** | 加在 embedding（或 attention）上的固定偏置。本项目的核心研究对象 |
| **对称性破缺** | `b = 0` 时，把 embedding 整体旋转一下模型行为不变（旋转对称性）。`b ≠ 0` 就把这个对称性打破了 |
| **val loss（验证损失）** | 在没训练过的数据上的预测误差。**越低越好** |
| **train loss（训练损失）** | 训练数据上的误差，参考用，不能和验证损失直接比大小 |
| **BLEU** | 机器翻译质量打分，**越高越好**（0~100）。短训练里通常是 0，不用慌 |
| **epoch（轮）** | 把训练数据完整过一遍叫一轮 |
| **参数量** | 模型大小。越大通常越好，但越慢 |
| **EGD** | 本项目的优化器：能量守恒下降法。对照的是常规的 AdamW |
| **attention** | Transformer 内部的「注意力」机制，bias 就是加在它的 q/k/v 上的 |

---

## 五、想更深入了解

- **项目背景、原理、完整参数表**：看 `README.md`（英文）。
- **所有命令行选项**：`python main.py train --help`
- **代码结构**：`symbreak_transformer/`（每个文件一个模块，encoder 和 decoder 是分开的）
- **测试**：`python -m pytest tests -q`（144 个测试）

> 内部实现可以很复杂（模型、优化器、对称性度量都拆成了独立文件），
> 但**你日常只需要 `run.bat` + 数字**。想改设置就编辑 `my_config.py`。
