# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.ipynb_checkpoints/

# Test / tooling caches
.pytest_cache/
.vscode/
# pytest leaves these behind when it cannot write its own basetemp
pytest-cache-files-*/

# Experiment output
/runs/
wandb/
*.pt
*.pth

# What the menu remembers for you (generated, never edited by hand)
/run_settings.json

# Downloaded corpora, cached raw files and tokenizers.
# Anchored to the repository root on purpose: an unanchored `data/` would also
# hide `symbreak_transformer/data/`, which is source code.
/data/
