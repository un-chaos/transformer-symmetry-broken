# Runtime requirements for transformer_sym (CPU-only research repo).
# Minimum versions match what is installed and verified on this machine:
# Python 3.13.14, torch 2.13.0+cpu, numpy 2.5.1, PyYAML 6.0.3, tqdm 4.69.1,
# pandas 2.2.3, matplotlib 3.11.1, pytest 9.1.1.
#
# There is no GPU and no CUDA on the target box: the torch wheel is the +cpu one.
torch>=2.13.0
numpy>=2.5.1
PyYAML>=6.0.3
tqdm>=4.69.1
pandas>=2.2.3
matplotlib>=3.11.1
pytest>=9.1.1

# optional:
# The data pipeline does NOT need HuggingFace's `datasets` or `transformers`.
# data/download.py uses urllib against the hf-mirror.com endpoint directly and
# parses JSON Lines itself, so neither package is required (and `datasets`
# would pull in a large dependency tree for no benefit here).
#   datasets     -- NOT required
#   transformers -- NOT required
# BLEU is implemented in-tree (transformer_sym/evaluate.py), so `sacrebleu`
# is NOT required either; install it only if you want to cross-check our
# scores against the reference implementation.
#   sacrebleu    -- optional (cross-checking only)
